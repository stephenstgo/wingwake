# WingWake Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================================================
# Convex Configuration
# ============================================================================
# Get these values from your Convex project dashboard:
# https://dashboard.convex.dev

# Convex Deployment URL
# Run `npx convex dev` to get your deployment URL
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Optional: Convex Auth Token (for server-side operations)
# CONVEX_AUTH_TOKEN=your_auth_token

# ============================================================================
# Convex Auth JWT Keys (Set in Convex Dashboard, not here!)
# ============================================================================
# These must be set in your Convex deployment's environment variables:
# - JWT_PRIVATE_KEY: RSA private key (generated by generateKeys.mjs)
# - JWKS: JSON Web Key Set (generated by generateKeys.mjs)
# 
# To generate and set:
# 1. Run: node generateKeys.mjs
# 2. Copy the output to Convex Dashboard → Settings → Environment Variables
# OR use: npx convex env set JWT_PRIVATE_KEY "..." and npx convex env set JWKS '...'
# 
# These are NOT set in .env.local - they must be in Convex's environment variables!

# ============================================================================
# Legacy: Supabase (for data migration only)
# ============================================================================
# These are only needed if you want to migrate data from Supabase
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# ============================================================================
# Application Configuration
# ============================================================================
# Node environment (development, production, test)
NODE_ENV=development

# ============================================================================
# Future: Email Service (Phase 3)
# ============================================================================
# RESEND_API_KEY=your_resend_api_key
# EMAIL_FROM=noreply@yourdomain.com

# ============================================================================
# Future: Payment Processing (Phase 2)
# ============================================================================
# STRIPE_SECRET_KEY=your_stripe_secret_key
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# ============================================================================
# Future: Error Monitoring (Phase 3)
# ============================================================================
# SENTRY_DSN=your_sentry_dsn

# ============================================================================
# Setup Instructions
# ============================================================================
# 1. Copy this file: cp env.example .env.local
# 2. Fill in your Convex deployment URL (run `npx convex dev` to get it)
# 3. Never commit .env.local to version control (it's in .gitignore)
# 4. For production, set these in your hosting platform (Vercel, etc.)
